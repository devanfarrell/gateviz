import React from 'react';
import posed from 'react-pose';

const Inputs = ({ stateString }) => {
	return (
		<div>
			<div>Inputs</div>
			<div className="inputs-wrapper">stuff</div>
		</div>
	);
};

export default Inputs;
